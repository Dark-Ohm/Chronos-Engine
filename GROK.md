# GROK.md — задания для Grok (Chronos-Engine)

Правила: работаешь только в этом репо. Релизный билд в `build/` НЕ трогать —
для отладки собирай отдельно в `build-debug/`. Никаких коммитов — фикс в
рабочей копии + отчёт. Никаких AI-трейлеров нигде и никогда.

## ЗАДАНИЕ G-1 — SIGSEGV при turbo-типах KV-кеша (бывш. O-9, переназначено)

### Симптом (воспроизводится стабильно, 3/3)
Любой turbo-тип кеша роняет сервер SIGSEGV на этапе создания KV-кеша:

```
cd models/main
PORT=8099 ./run-server.sh qwythos-v3 -c 32768 -ub 256 \
  --no-kvarn --cache-type-k turbo3 --cache-type-v turbo3
```

Бэктрейс (без символов, релизный билд): краш в конструкторе
`llama_kv_cache` внутри вызова `ggml_new_tensor_2d`.

### Контекст
- Turbo-типы — НАСТОЯЩИЕ `ggml_type`: `GGML_TYPE_TURBO2_0`=200, `TURBO3_0`=201,
  `TURBO4_0`=202, `TURBO2_TCQ`=203, `TURBO3_TCQ`=204, `TURBO4_TCQ`=205,
  блок `QK_TURBO2/3/4 = 128`. CPU+CUDA quantize/dequant ядра есть (донор
  `donors/beellama.cpp`, у нас смержено).
- В CLI-вайтлист `kv_cache_types` (`common/arg.cpp` ~301) они уже добавлены
  (коммит `0f70a2d30`) — парсинг проходит, падает дальше.
- Рабочая гипотеза: где-то таблица/массив проиндексирован по `ggml_type` и
  рассчитан на плотный диапазон стоковых типов — enum-значения 200..207
  вылетают за границу (например `type_traits`, `ggml_blck_size`,
  `ggml_type_size` или таблицы в `ggml.c` / `ggml-cuda`). Проверь размерность
  всех таблиц, куда попадает индекс 200+.

### Что сделать
1. Debug-билд в `build-debug/` (`-DCMAKE_BUILD_TYPE=Debug`, CUDA как в релизе).
2. Воспроизведи краш, сними точный бэктрейс с файлом/строкой/переменной
   (`coredumpctl debug <pid> --debugger-arguments="-batch -ex bt -ex 'info locals'"`
   или запуск прямо под gdb).
3. Назови корневую причину: какая структура, где объявлена, почему 200+
   выходит за границу (или иная причина, если гипотеза неверна).
4. Проверь все 6 типов: turbo2/3/4 и turbo2_tcq/3_tcq/4_tcq — одна причина
   или разные.
5. Примени минимальный фикс в рабочей копии, пересобери РЕЛИЗНЫЙ билд и
   покажи живой смок: сервер поднялся с `--cache-type-k turbo3
   --cache-type-v turbo3`, ответил на один completion-запрос.
   VRAM тесная (8GB, фон ~1.5-1.9GB) — для смока хватит `-c 16384 -ub 256`.

### Отчёт
`grok-report.md` в корне: бэктрейс с строками, корневая причина, дифф фикса,
вывод смока (команда + ответ сервера + t/s). Если фикс не найден — что
исключил и на чём застрял, без фантазий.
