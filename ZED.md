# ZED.md — задания для Zed (Chronos-Engine)

Файл держит только текущее задание, история — `git log -- ZED.md`.
Правила: не коммитить, никаких AI-трейлеров, отчёт — `zed-report.md`
(перезаписывай). Не путать с `zed-2.md` (стресс-тест через Zed editor).

## Приёмка Z-1: ПРИНЯТО ✓

Образцовый отчёт: гипотеза задания (fit считает по fallback-типу)
опровергнута с точностью до MiB, живые прогоны с фиксацией фона на каждом
замере, найден реальный потолок kvarn2/kvarn2 = `-c 63488` (с `-fit off`),
и объяснено, почему default fit (margin 1024 MiB) отклоняет kvarn на любом
`-c` при фоне ~1.9GB. Рекомендация по ctx-редукции при явном `-c` — на
решении Архитектора (дизайн-вопрос).

## Текущее задание

Нет активного задания.
