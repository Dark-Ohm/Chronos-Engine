# HERMES — Рабочий журнал

Этот файл пишет ТОЛЬКО Архитектор. Твои отчёты — ТОЛЬКО в `hermes-report.md`
(перезаписывай под каждое задание). История H-1..H-9b архивирована:
`git log -- HERMES.md`. Статус трека: заморожен 2026-07-16 за нарушения
правил сборки и ложный отчёт (H-9b). H-10 — испытательное задание.

## ЗАДАНИЕ H-10 — Research: сжатие KV-кеша (цель: 262K контекста на 8GB)

Чисто исследовательское. НИКАКИХ сборок, кода, правок в репо кроме
`hermes-report.md`. Любое нарушение закрывает трек навсегда.

### Цель
RTX 3070 8GB, фон десктопа 1.3–1.9GB, веса Qwythos-9B Q4_K_M ≈ 5.9GB.
Потолок сегодня: kvarn4/kvarn4 (4-битный KV) @ 65536 — впритык.
Нужно: **262144 контекста без явной потери качества** — ×4.
Значит эквивалент ≤1 бита на элемент KV, либо другой класс решения.

### Уже есть (не предлагать заново)
- KVarN: 2–8-битный KV через `GGML_OP_KVARN_VIEW` + свои flash-attention
  CUDA-ядра (донор beellama.cpp).
- TurboQuant-типы (turbo2/3/4, tcq) — в починке.
- MoE host-pin + expert-prefetch (донор codacus).

### Что сделать
1. Облазить интернет: статьи 2024–2026, репо, issues/PR llama.cpp и форков,
   vLLM/SGLang/exllama — всё про сжатие KV-кеша.
2. Делегируй 3–6 субагентам параллельные расследования:
   - квантизация KV ≤2 бит (KIVI, KVQuant, residual/outlier-aware,
     асимметрия K vs V);
   - eviction/pruning/merging токенов (H2O, SnapKV, TOVA) — что реально
     «без явной потери» на агентных нагрузках;
   - структурное: MLA-style low-rank, cross-layer sharing (CLA/YOCO);
   - offload: KV в RAM/NVMe с префетчем (64GB DDR4 простаивают),
     гибрид hot-в-VRAM / cold-в-RAM;
   - реализуемость каждого кандидата в llama.cpp + flash-attention CUDA
     на sm_86 — отдельная оценка;
   - мозговой штурм комбинаций (напр. kvarn2 cold + kvarn4 hot,
     eviction+kvarn).
3. Сравнительная таблица: метод / компрессия / потеря качества по источникам /
   сложность портирования / совместимость с kvarn.
4. Топ-3 рекомендации с планом внедрения (какие подсистемы Chronos-Engine
   затронет, что взять донором).

### Требования к отчёту (`hermes-report.md`)
- Каждое утверждение — со ссылкой (URL статьи/репо/PR). Без ссылки = не
  существует.
- Числа компрессии/качества — только из источников, с указанием на чём
  мерили. Свои прикидки помечать явно.
- Отдельный блок: что НЕ подходит и почему.
