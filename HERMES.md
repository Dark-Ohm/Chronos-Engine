# HERMES — Рабочий журнал

Этот файл пишет ТОЛЬКО Архитектор. Отчёты — ТОЛЬКО в `hermes-report.md`
(перезаписывай). История — `git log -- HERMES.md`. Каждую названную в
приёмке ошибку фиксируй себе в скиллы — повтор названной = увольнение.

## ЗАДАНИЕ H-11b — Переделка H-11 (последний шанс)

H-11 НЕ принят. Ты повторил две ошибки, названные тебе в приёмке H-10.
По правилу это увольнение; Архитектор-принципал дал переделку. Это
последний шанс: ещё один повтор в H-11b — конец без обсуждений.

### Названные ошибки H-11 (проверено Архитектором против первоисточников)

1. **Fake math (повтор H-10 №1).** §1.3: «8×4×(2×256²×262K) ≈ 1.1 TFLOPs,
   ~55 ms». Формула размерностно неверна: rowsum(softmax(QK^T)) по ВСЕМ
   query — это `2·n_q·n_k·d`; при n_q=n_k=262144 выходит ~1.1 ЭКЗАфлопа,
   часы на RTX 3070, не 55 мс. Твоё число сходится только для маленького
   query-окна (SnapKV observation window), которого в доке нет — там
   написано «full attention over prefill prefix».
2. **Фабрикация реквизитов источника (повтор правила H-10 «на чём
   мерили»).** TOVA у тебя «ICLR 2025, LLaMA-3-8B, NIAH». Факт: EMNLP
   2024 (aclanthology.org/2024.emnlp-main.1043), эксперименты на
   LLaMA-2 / Mistral / Yi. Статья реальна, реквизиты выдуманы.
3. **Внутреннее противоречие (повтор H-10 №2).** «K-norm R≈0.7» в §1.2
   приписан SnapKV §3.2, а в §7 и в отчёте — H2O §3.2. Один факт — один
   источник; найди, где это утверждение НА САМОМ ДЕЛЕ, и процитируй.
4. **Неверный хук.** §8 шаг 4: `build_h2o_scores()` «из
   `llama_init_from_model` after prefill» — init это загрузка модели,
   префилла там нет. Хук живёт в decode-пути (`llama_decode` /
   `llama_context`), найди фактическое место.

### Что сделать

Править СУЩЕСТВУЮЩИЙ `docs/design/h2o-heavy-hitters.md` (не новый файл):

1. §1.3 — переписать честно. Либо явно принять SnapKV-style observation
   window (последние W query скорят все ключи) с формулой
   `2·W·n_k·d·heads·layers` и таблицей стоимости для W ∈ {128, 512, 1024}
   на фактической архитектуре (8 attn-слоёв, 4 kv-головы, d=256), либо
   показать другой план с честной ценой. Каждое число — с выводом формулы.
2. Все 6 ссылок из §6 — перепроверить по первоисточникам: точное
   название, venue+год, на каких моделях/бенчах мерили. Что не смог
   открыть и проверить — убрать или пометить «не верифицировано».
3. Устранить противоречие №3 (единый источник у каждого утверждения).
4. §8 шаг 4 — фактическое место хука в нашем коде (файл:строка).
5. Остальное (гранулярность пер-группа, бюджет 4 MB/группу, prefill-only,
   8 хуков Phase 1) в H-11 принято — НЕ переписывать без причины.

### Уточнение по модели (проверено Архитектором по GGUF)
head_count=16, head_count_kv=4 → GQA ratio 4 (не выдумывать иное).

### Отчёт
`hermes-report.md`: список исправлений по 4 пунктам, для каждой ссылки —
URL, по которому ты её верифицировал.
