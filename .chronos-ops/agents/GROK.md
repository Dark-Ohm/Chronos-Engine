# GROK.md — задания для Grok (Chronos-Engine)

Файл держит только текущее задание, история — `git log -- .chronos-ops/GROK.md`.
Правила: релизный `build/` пересобирать можно, отладка — в `build-debug/`.
Не коммитить. Никаких AI-трейлеров. Отчёт — `grok-report.md` рядом с этим
файлом (перезаписывай).

## Приёмка G-4: ПРИНЯТО ✓ — нашёл критический баг

G-4 (без ollama, HEAD `88d4a06c1` + Z-2/Z-3 `cc4d8d597`) закрыл гэп §4
из G-3 (kvarn4@63488 OOM был бюджетом VRAM, не багом — подтверждено),
но нашёл **более серьёзную проблему**: `--kv-hot-size` ломает retrieval
даже ВНУТРИ hot-окна (короткий A/B: промпт 1763 токенов << hot 4096,
без флага игла находится, с флагом — нет, мусор). Это не деградация по
дизайну Phase 1 (которая ожидается ЗА пределами окна) — это баг
корректности в SWA/`mat_idxs`-обвязке из Z-3. Методология сильная
(короткий A/B изолировал «баг корректности» от «дизайн режет старое»).

Задание передано Zed (Z-4, `ZED.md` в корне — его код).

## Пока свободен

Следующее задание — после Z-4 (может понадобиться повторная валидация
после фикса retrieval). Если требуется независимая работа параллельно —
спроси Архитектора.
