# T003c — Phase 2 H2O: scoring kernel + граф-узел + selection/retention — ЗАБЛОКИРОВАН

**Роль:** `src/llama-graph.cpp` (граф-узел), `ggml/src/ggml-cuda/kvarn.cu`
(scoring kernel — НЕ `src/llama-kvarn.*`, см. поправку ниже), новый файл
для selection-логики.
**Приоритет:** не брать в работу — см. «Блокеры».
**Источник:** `docs/design/h2o-heavy-hitters-PHASE2-SPEC.md`, approach A
(SnapKV-style observation window). Пересобран после ревью (Архимаг,
2026-08-15) — снятая версия содержала математическую ошибку,
унаследованную из самой канонической SPEC. Формула теперь чинится
отдельным тикетом [[T000]] (Волна 0), не здесь.
**Зависимости:** [[T000]] принят (SPEC-формула исправлена), T003a И
T003b оба приняты, [[T010]] принят.

## Блокеры (по порядку разблокировки)

1. [[T000]] — SPEC содержит математически неверную формулу scoring
   (`rowsum(softmax(QK^T))` тождественно равен 1, ноль информации о
   ключе) — выведен в отдельный тикет (Волна 0, конфликт по файлу с
   T003a). Читать исправленную SPEC здесь, не оригинальную §1.3.
2. [[T010]] принят (живой NIAH) + подтверждённая prefetch-инфраструктура
   (T003b принят — scoring строится поверх его prefetch/view, не
   параллельно с ним).

## Что сделать (после снятия всех блокеров)

- `src/llama-graph.cpp`: узел графа для H2O attention score tracking
  во время prefill (approach A — scoring pass в конце prefill,
  наблюдательное окно `W`).
- **Scoring-кернель — в `ggml/src/ggml-cuda/kvarn.cu`**, рядом с
  остальной KVarN CUDA-инфраструктурой (`ggml_kvarn_view` и соседи),
  **не в `src/llama-kvarn.*`** (это host-side C++ обвязка, не место для
  CUDA-кернеля scoring pass) — снятая версия тикета путала расположение.
  Имя (`ggml_kvarn_h2o_score` или актуальное) сверить со SPEC на момент
  старта. Кернель считает исправленную формулу (column-sum по ключам
  в окне W, не rowsum).
- Heavy-hitter selection + retention логика (новый файл или расширение
  существующего) — какие группы остаются «горячими» после prefill, на
  основе агрегированных per-group `score[key]`.
- Fallback на approach C (K-norm приближение, `‖K_i‖²`), если approach A
  окажется слишком медленным на живых замерах.

## Верификация

- **Quality/PPL — не 262K напрямую.** SPEC (Verification, Phase 2) сама
  требует сравнение с full-attention baseline **на 32K с экстраполяцией**
  (полный full-attention проход на 262K — то самое A1, что сама же SPEC
  отклонила как непрактичный, ~4.5 PFLOPs). Снятая версия тикета
  требовала «PPL в пределах 1% от full-attention на 262K» напрямую —
  внутреннее противоречие с собственным обоснованием SPEC, почему
  выбран approach A вместо A1. Верно: PPL в пределах 1% на 32K с H2O
  retention, экстраполяция на 262K по методике SPEC.
- **PCIe bandwidth и DMA/compute overlap — это T003b, не сюда** (снятая
  версия тикета дублировала эту метрику здесь; она измеряет
  prefetch-инфраструктуру, не scoring-логику).
- Живой NIAH после каждого куска этого среза (не только в конце) — урок
  раунда 6: «не падает» ≠ «работает».

## Не открывать этот файл на редактирование, пока Блокер №0 (формула в SPEC) не исправлен, T003a и T003b не приняты, и T010 не закрыт живым прогоном.
