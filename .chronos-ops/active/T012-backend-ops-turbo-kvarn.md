# T012 — test-backend-ops: покрытие turbo / kvarn (не дублировать FWHT)

**Роль:** `tests/test-backend-ops.cpp`.
**Приоритет:** P3 — suite зелёный по kvarn/turbo, потому что этих опов
там нет. FWHT как ggml-оп уже покрыт — см. ниже, не плодить второй набор.
**Источник:** выписано из `.chronos-ops/agents/CLINE.md` (задание C-3).
Поправка 2026-08-16: G-3 «ноль совпадений turbo|kvarn|WHT» неточна в
части WHT.
**Зависимости:** нет. Реализацию опов не трогать.

## Сначала прочитать, что уже есть

`// FWHT tests` уже стоят в двух местах:
- `tests/test-backend-ops.cpp:8588` — `test_mul_mat_hadamard`
- `tests/test-backend-ops.cpp:9597` — то же

Это `GGML_OP_MUL_MAT` с `ggml_mul_mat_set_hint(..., GGML_HINT_SRC0_IS_HADAMARD)`
(`test_mul_mat_hadamard` около :4174), не `ggml_turbo_wht` /
`GGML_OP_KVARN_WHT`. Прочитать оба блока и класс целиком. Второй набор
тех же Hadamard-matmul кейсов не добавлять — рост покрытия на уже
покрытом не считается.

`GGML_OP_KVARN_WHT` добавлять только если после чтения ясно, что это
другой контракт (отдельный оп, не тот же hint на mul_mat). Если тот же
трансформ уже гоняется через hint — пропустить и написать это в отчёт.

## Что сделать

1. Как suite регистрирует кейсы (`test_case` / `make_test_cases` —
   имя сверить по факту). Шаблон — уже покрытый custom-оп
   (`GGML_OP_FLASH_ATTN_EXT` или сосед), либо сами FWHT-кейсы выше.
2. Добавить адресно, не переписывая файл и не дублируя FWHT:
   - `GGML_OP_KVARN_VIEW` — forward на размерах, кратных
     `KVAR_N_GROUP=128`. Имя опа в `ggml/include/ggml.h`.
   - Quantize/dequantize round-trip turbo2/3/4_0 — существующим
     механизмом quant-тестов в этом файле, новый не выдумывать.
   - `GGML_OP_KVARN_WHT` — только по правилу из блока выше.
3. Оп только на CUDA, на CPU нет пути — написать в отчёт, не обходить
   хаком. Нет публичного API собрать граф с нужным опом — СТОП, в отчёт
   чего не хватает.

## Границы

Только `tests/test-backend-ops.cpp`. `ggml/src/...` не трогать.

## Верификация

Список добавленных кейсов (оп, размеры, типы). Явно: что из FWHT
прочитано и почему не дублировалось. Что не покрыто и почему.
Компилируется. Прогон suite — на приёмке, не здесь, если в тикете не
разрешено иное.
