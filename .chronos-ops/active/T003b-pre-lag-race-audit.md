# T003b-pre — аудит lag-race в async prefetch-примитиве (предусловие T003b)

**Роль:** `src/llama-kv-cache-kvarn.cpp` (только чтение/анализ + живой
тест, без изменения логики, если аудит не потребует фикса).
**Приоритет:** P2 — самостоятельная работа, не подпункт внутри T003b.
[[T010]] закрыт живым прогоном 2026-08-16 — **гейт снят, можно
стартовать.** Идёт параллельно с [[T003a]] (второй рукой) либо сразу
после него: файл `llama-kv-cache-kvarn.cpp` общий, но зоны разные
(T003a — только ctor, этот тикет — `enqueue_cold_offloads` и соседи);
одновременные коммиты в один файл не делать.
**Гейтит:** [[T003c]] — не по коду, а по механике: пин H2O-групп против
перезаписи в кольце и «длинный prefill теряет cold-копии» — это одно и то
же место. T003c не стартует, пока этот аудит не вынесет вердикт по
wraparound (п.3 ниже).
**Источник:** ревью (Архимаг, 2026-08-15) — извлечено из T003b «Блокер
№2» в отдельный тикет: «если его не поставить в расписание явно, он
станет неожиданным критическим путём перед волной 3».
**Зависимости:** [[T010]] принят (живой NIAH внутри окна — та же
переверификация, что открывает Волну 2 целиком; не дублировать, взять
результат T010).

## Контекст

`src/llama-kv-cache-kvarn.cpp` (район строки 1022 на HEAD, сверить по
факту) содержит собственный предупреждающий комментарий про
существующий async export-примитив (НЕ полноценные prefetch slots —
это уже отдельно исправлено в T003b, не путать снова):

> Timing: a group is only queued once `tail_groups` MORE groups have
> been seen after it [...] This lag is a conservative assumption about
> the kernel's flush timing, **not verified bit-for-bit against the
> CUDA/CPU kvarn_store implementation; see the report's caveats before
> relying on this for Phase 2/3 prefetch.**

T003b строит prefetch slots/events НАД этим примитивом. Если допущение
о лаге неверно, T003b унаследует таймингованный баг, который проявится
не сразу и не детерминированно — ровно тот класс проблем, который
дорого ловить постфактум внутри уже написанного T003b кода.

## Что сделать

1. Найти исходный отчёт, на который ссылается комментарий («see the
   report's caveats») — если он существует в `.chronos-ops/dump/` или
   истории — прочитать целиком, не полагаться на пересказ в комментарии.
2. Подтвердить или опровергнуть допущение о `tail_groups`-лаге:
   бит-в-бит (или численно, если бит-в-бит не имеет смысла для этого
   формата) сверить фактическое время флаша CUDA/CPU `kvarn_store` с
   предполагаемым лагом. Живой замер, не чтение кода как единственное
   доказательство.
3. **Wraparound на длинном prefill — в scope, не «на потом».** Тот же
   блок комментария (`llama-kv-cache-kvarn.cpp:1030-1035`) описывает
   вторую, отдельную проблему:

   > a single `llama_decode()` call whose ubatches flush more groups than
   > `n_groups_per_stream` [...] can wrap the GPU ring before the next
   > `llama_decode()` drains `pending_cold_offloads`, **losing the
   > overwritten groups' cold copies.** Typical server usage chunks
   > prefill by `n_ubatch`, which drains between chunks and avoids this
   > in practice.

   «Avoids this in practice» — предположение, а не проверка. Требуется:
   воспроизвести живьём (один `llama_decode` с prefill-чанком больше
   кольца), показать, теряются ли cold-копии, и если да — зафиксировать
   границу, за которой это происходит. Приёмка «лаг ок» БЕЗ вердикта по
   wraparound не принимается: T003b унаследует потерю данных, а T003c
   будет пинить группы в кольце, которое их молча роняет.
4. Если допущение подтвердилось — задокументировать явно (комментарий
   в коде уже не «conservative assumption», а «verified», с датой и
   методом проверки). Отдельно — то же для wraparound.
5. Если допущение НЕ подтвердилось — здесь же фикс лага (не откладывать
   в T003b) ИЛИ явная эскалация архитектору, если фикс требует более
   широкого изменения формата.

## Верификация

- Живой прогон, не только чтение источника.
- **Два независимых вердикта**, оба обязательны: (а) `tail_groups`-лаг,
  (б) wraparound на prefill-чанке больше кольца. Один без другого —
  тикет не принят.
- Результат (подтверждено / опровергнуто+исправлено) — явно записан в
  этот файл или отчёт рядом; [[T003b]] и [[T003c]] ссылаются на
  результат, не переоткрывают вопрос.
