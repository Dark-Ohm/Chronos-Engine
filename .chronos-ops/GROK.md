# GROK.md — задания для Grok (Chronos-Engine)

Файл держит только текущее задание, история — `git log -- GROK.md`.
Правила: релизный `build/` пересобирать можно, отладка — в `build-debug/`.
Не коммитить. Никаких AI-трейлеров. Отчёт — `grok-report.md` (перезаписывай).

## ЗАДАНИЕ G-3 — Формальная валидация (Фаза 7 из ARCHITECTURE.md)

Код не менять вообще (найдёшь баг — в отчёт, не чинить). Ты валидируешь
HEAD `d3680c912` как есть. GPU в твоём распоряжении: перед тяжёлыми
прогонами убей llama-server (`pkill -x llama-server`) и смотри фон
(`nvidia-smi --query-gpu=memory.used --format=csv,noheader`).

### 1. Parity: test-backend-ops
Эталон предыдущих раундов: **13994/13994 passed**. Прогнать
`./build/bin/test-backend-ops` (полный), зафиксировать точное число
passed/failed. Любой fail — список имён упавших опов дословно.
Отдельно грепни результаты по turbo/kvarn/WHT-опам — после G-2 (CPU
TURBO_WHT + quantize turbo2/3) могли появиться новые покрытия или новые
фейлы — это самое интересное место.

### 2. Бенч vs baseline
Baseline (Фаза 1, полный offload, из CLINE.md): **pp512=2298, tg64=61**.
`llama-bench` на qwythos-v3, три конфига:
- без kvarn (f16 KV) — сверка с baseline, регрессия >5% = красный флаг;
- kvarn4/kvarn4;
- turbo3/turbo3.
Контекст/параметры подбери по VRAM (фон!), главное — все три конфига
в ОДИНАКОВЫХ условиях, числа в таблицу.

### 3. PPL-спот-чек
`llama-perplexity` на небольшом корпусе (wikitext-2 сэмпл или что есть
локально; если корпуса нет — скачай стандартный wikitext-2-raw в
`/tmp`), контекст 8192: f16 vs kvarn4 vs turbo3. Дельта PPL kvarn4 и
turbo3 относительно f16 — с точными числами. Ожидание по источникам:
turbo3 ≈ нейтрален, kvarn4 небольшая дельта. Сильное расхождение с
ожиданием — не замалчивать.

### 4. Длинный контекст (облегчённый NIAH)
kvarn4 `-c 63488 -fit off`: промпт ~50K токенов (сгенерируй текст с
«иглой» — фактом в начале), вопрос про иглу в конце. Ответил/не ответил,
дословный ответ. То же на kvarn2 — сравнить.

### Отчёт
`grok-report.md`: таблицы всех прогонов с командами запуска, фон VRAM
на каждый замер, вердикт по каждому из 4 пунктов (PASS/FAIL/ВОПРОС).
Найденные баги — отдельным списком, без починки.
