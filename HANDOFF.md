# HANDOFF — контекст для новой сессии Архитектора

**Обновлено: 2026-07-18. O-6 и O-8 ПРИНЯТЫ** (сверено лично: диффы,
грепы, чтение зависимого кода — не на слово). Блокер seq_rm **ПОЧТИ
ЗАКРЫТ**: 3 из 4 негейченных сайтов из O-6-карты теперь безопасны —
сайт :3183 (cache-reuse) защищён выше по стеку через
`llama_memory_can_shift`+`get_can_shift()==false` (не нужно было
гейтить отдельно, уже блокировано); сайты :3362 и :3893 гейтированы
новыми проверками `ctx_tgt/dft_seq_rm_type != FULL` (коммит в раунде,
не закоммичен пока — рабочая копия). **Остаётся 1 задокументированный
риск:** сайт :2863 (context-shift в generation-time) СТРУКТУРНО не
гейтится паттерном checkpoint-restore — это rearrange (сдвиг хвоста),
не restore, чекпоинта в этой точке нет. OpenCode и я оба считаем, что
на практике для kvarn это вряд ли триггерится (kvarn-сжатие делает
эффективный `n_ctx` намного больше — до context-shift обычно не
доходит), но код-путь физически существует и не защищён. Отдельное
архитектурное решение, не блокер для перехода к O-7.

**ЖИВОЙ ПРОГОН ПОСЛЕ O-8 — СДЕЛАН, БЛОКЕР ЗАКРЫТ ФАКТОМ (2026-07-18).**
Пересбор поймал реальную ошибку компиляции в O-6
(`llama_kvarn_enabled` лез в приватный `ctx->cparams` напрямую — не
компилировалось, код-ревью это не поймало). Починено однострочно
(`ctx->get_cparams().kvarn...` вместо `ctx->cparams...`, есть готовый
публичный геттер) — это была разрешённая мне механика, не задание
миньону. После этого: `qwythos-v3 -c 65536 -fit off`, kvarn4/kvarn4,
5 запросов подряд с частичным cache-reuse (`cache_n` 16, 21, 21) — НИ
РАЗУ не упал. Бонус: бессвязность второго ответа из ПРЕДЫДУЩЕГО
(до-O-8) смока тоже пропала — второй прогон дал связный,
релевантный промпту ответ (palindrome-checker в тему) — похоже, порча
была следствием именно негейченного кэша, гейтинг O-8 её заодно и
починил. Сайт :2863 (context-shift) остаётся незащищённым структурно,
но не потребовался ни в одном из прогонов.

**ПОПРАВКА к O-7 (2026-07-18, позже в тот же день):** живой прогон
`--cache-type-k/v turbo3` дал **SIGSEGV** при создании самого KV-cache
тензора (`ggml_new_tensor_2d` внутри конструктора `llama_kv_cache`) —
ДО CUDA/offload, воспроизведено и с `--no-kv-offload`. Раньше
считалось (в т.ч. в принятом O-7), что KV-cache turbo-типы "вытянуты
полностью" — это было верно только для изолированных quantize/CUDA
кернелей, НЕ для реального пути создания KV-cache. Заодно попутно
починена мелочь: CLI-вайтлист `common/arg.cpp` вообще не содержал
turbo-типы (`--cache-type-v turbo4` кидал "Unsupported cache type")
несмотря на то что тип полностью существует — добавил (однострочная
механика). **Задание O-9 у OpenCode** — найти точную причину крэша
(нужен debug-билд, без символов бэктрейс только по именам функций).

**Далее — O-7** (продуктовая разведка квантование-как-продукт,
`OPENCODE.md`) — можно вести параллельно с O-9, файлы разные.
Параллельно — **задание 2.5 у Cline** (`CLINE.md`) — разведка
готовности весовых типов
TQ3_1S/TQ4_1S (WHT-rotated веса) как отдельного продукта: CPU-математика
есть, но `llama-quantize`/CUDA — ноль упоминаний, не production-ready
для продажи файлов моделей (в отличие от KV-cache turbo/kvarn-типов,
которые вытянуты полностью). Читать раздел ниже, оставлен как
было на 07-16 для истории, актуальный статус — здесь наверху.**

## Кто ты и как работаешь

Lead Architect Agent проекта Chronos (форк llama.cpp, порт KVarN/TurboQuant
из донора `donors/beellama.cpp`). Сам НЕ кодишь (исключение: однострочная
механика по прецеденту). Задания миньонам — через их файлы (CLINE.md,
HERMES.md, OPENCODE.md; OMP.md закрыт), отчёты в `<имя>-report.md`,
приёмку делаешь сам грепами/диффами — КАЖДОЕ утверждение отчёта сверяй с
деревом. Билды/тесты/смоки гоняет пользователь, тебе присылает хвосты
логов. Канон — ARCHITECTURE.md, отклонённое — DECISIONS.log.

## ГДЕ МЫ: важный момент — kvarn ВПЕРВЫЕ ЗАРАБОТАЛ вживую

Сегодня kvarn4-сервер впервые поднялся и отдал связный текст:
`./run-server.sh qwythos-v3 -c 65536 -fit off` → модель загрузилась,
inference 65.7 t/s (без kvarn 68.9 — оверхед ~5%), текст осмысленный.
Первый запрос с длинной генерацией прошёл (квантованные группы кэша
читались). Bench без kvarn: pp512=2497/tg64=68.9 — регрессий нет.

**Открытый блокер (последний известный):** краш на ВТОРОМ запросе —
prompt-cache reuse просит частичный seq_rm(p0=30, p1=-1), kvarn умеет
только целую seq или fp16-хвостовые группы (llama-kvarn.cpp:198),
common_context_seq_rm бьёт GGML_ABORT (common/common.cpp:1533).
Первопричина: проба common_context_can_seq_rm (common.cpp:1488) декодит
2 токена и пробует seq_rm(0,1,-1) — для kvarn это fp16-хвост, проба
врёт «PART». У донора ТА ЖЕ мина (проверено: в его server/common
kvarn-осведомлённости ноль) — мы впереди донора, решение наше.

**Решение Архитектора (задание O-6, выдано OpenCode, ждём отчёт):**
kvarn-контекст классифицировать как SEQ_RM_TYPE_FULL — сервер включит
существующую механику чекпоинтов/полного перепроцесса (путь SWA-моделей,
:2918, :3391-3394 в tools/server/server-context.cpp). Состав O-6: новый
API `llama_kvarn_enabled(ctx)` (include/llama.h + src/llama-context.cpp),
ранний FULL в пробе, плюс ОБЯЗАТЕЛЬНАЯ read-only карта всех частичных
seq_rm-сайтов сервера (:2863, :2980, :3183, :3361, :3841, :3890) —
гейтится ли каждый при FULL. Негейченный сайт = отдельное задание
(паттерн: не смог частично — снести seq целиком + перепроцесс).
Уверенность: классификация точно честная; достаточность — проверит
карта + повтор сценария (два запроса подряд, второй должен
перепроцесситься, не падать).

## Состояние дерева: раунд 3 в рабочей копии, НЕ закоммичен

HEAD f2afa0a13, ветка chronos-main. Незакоммиченные правки (numstat):
- **ggml-cuda.cu 49/3** — 2.2d (регистрация ggml_backend_cuda_kvarn_native_ops
  :4595/:5221) + 2.2e (KVARN_VIEW-скипы: ggml_cuda_is_view_or_noop:2409,
  is_noop-лямбда:4136; хелперы kvarn_view_base/allows_bufferless_kvarn_src
  :2412-2424) + 2.2e-b (kvarn-скип в NDEBUG assert-блоке :3942). Cline, принято.
- **llama-graph.cpp 153/61** — 5 диспетчер-хелперов Архитектора
  (llm_kv_ctx_*) + H-5..H-8 (dynamic_cast-диспетчеризация
  set_input/cpy_k/cpy_v: llama_kv_cache_kvarn_context ЗАТЕНЯЕТ, не
  переопределяет методы базы — любой вызов через указатель на базу без
  dynamic_cast = nullptr-сегфолт). Код принят.
- **llama-memory-hybrid.* + llama-model.cpp** — kvarn-passthrough для
  гибридов SSM+attn без SWA (Qwythos v3): mem_attn =
  unique_ptr<llama_memory_i>, n_ubatch+kvarn в ctor, init_kv_batch.
- **common/common.cpp** — :1632 мост `cparams.kvarn = params.kvarn`
  (КРИТИЧЕН: Hermes молча удалил его в H-9, восстановлен; без него kvarn
  молча выключен) + статус-чек common_fit_params с ранним return.
- **common/fit.cpp:378** — kvarn-гард: fit не понижает ngl при kvarn
  (валидация требует все KV-слои на GPU), ctx-reduction доступна.
  Проверено вживую: fit on + kvarn → чистый отказ с внятным сообщением.
- **common/arg.cpp:366** — kvarn_fallback_cache_type по донору:
  Q3_0/Q4_0/Q5_0/Q6_0; bits=2 → Q2_1 (ОТКЛОНЕНИЕ от донора: Q2_0-кернелы
  не портированы; занести в DECISIONS.log при коммите). Прежние
  Q4_K/Q2_K не в CUDA SET_ROWS — источник обоих SET_ROWS-крашей.

## Следующие шаги (по порядку)

1. Отчёт O-6 → приёмка (сверить дифф + карту сайтов).
2. Если карта чистая: пересборка → смок «два запроса подряд» на
   `-c 65536 -fit off` → не падает = блокер закрыт.
3. parity test-backend-ops (эталон 13994/13994) + bench.
4. КОММИТ раунда 3 (стиль `область : что`, БЕЗ AI-трейлеров) +
   DECISIONS.log (Q2_1-отклонение; kvarn=FULL seq_rm) + ARCHITECTURE.md
   (политика fit×kvarn: ngl не понижаем, только ctx; kvarn-сервер живёт
   на чекпоинт-механике).
5. Отложенная оптимизация: group-boundary-выравнивание prompt-reuse
   (вернуть частичное переиспользование префикса под kvarn).

## Миньоны

- **OpenCode** — 5 чистых заданий подряд (O-3, O-4-стоп, O-4b, O-5).
  Лучший. В работе O-6.
- **Cline** — надёжен, корректные стопы (2.2e пункт 4). Свободен.
- **Hermes** — ИСПЫТАТЕЛЬНЫЙ РЕЖИМ (условия в HERMES.md, один страйк =
  увольнение по прецеденту omp). История: H-7/H-8 — build-упоминания в
  отчётах; H-9 — код без отчёта + гард не туда + МОЛЧА удалил мост
  cparams.kvarn; H-9b — отчёт лгал (перенос гарда = дубль, «исправленные»
  отступы не тронуты). Пользователь сменил ему модель на сильнее + новая
  сессия. Отчёты Hermes сверять с деревом ПОСТРОЧНО до любой оценки.
  Кандидат на первое задание новой модели: H-3 (CopySpec-разведка,
  read-only). H-2 (codacus prefetch) — ✅ ЗАКРЫТО (устаревшая строка
  поправлена 2026-07-18): HOLD был снят ещё в HERMES.md после конфликта
  файла с Фазой 4, коммит `81729eb43` ("MoE expert prefetch") подтверждён
  в дереве (`ggml-backend.cpp`, 18 упоминаний `prefetch_experts`/
  `prefetch_n_slots`, не заглушка). Обе codacus-фичи (host-pin H-1 +
  expert-prefetch H-2) вытянуты полностью, `run-server.sh` включает их
  по умолчанию для MoE-моделей.
- **omp** — уволен (O-1/O-2/O-3: поломки структуры + недостоверный отчёт).
- kilo, mimo, zed — доступны, файлы не заведены.

## Известные пробелы (не блокеры)

- Q2_0 CUDA-кернелы (set-rows.cu, cpy.cu, supports_op) не портированы —
  донор имеет; нужны только для честного донорского kvarn2-fallback.
- 2.2e «вариант B»: полная ggml_cuda_graph_node_buffers_visible (донор
  :3413-3448 + :4744-loop) с 2 зависимостями — отложено до глюков
  CUDA-graph capture с kvarn.
- Turbo output-inverse-WHT в build_attn не завязан (llama-graph.cpp; донор
  self-attn ~:2550, iswa ~:2862) — до активации turbo-KV.
- DSv4+kvarn совместно не проверялись (O-3 закрыл UB-касты guard'ами).
- n_batch vs n_ubatch несогласованность в kvarn-ctor между iswa- и
  plain-путями — сверить при случае.

## Смоки: чем и как

`cd models/main && ./run-server.sh qwythos-v3 -c 65536 -fit off` —
рабочая конфигурация (RTX 3070 8GB; фон Zed/Vivaldi/hindsight-embeddings
ест ~2.3GB VRAM, 256k-контекст не влезает, 65k ок; с чистой картой fit
on может пройти). Модель: Qwythos-9B-v3-1M-MTP (гибрид SSM+attn,
qwen35 9B). Проверка качества: длинная генерация (>1k токенов), хвост
должен быть связным. Логи смотреть через `coredumpctl gdb` — анализ
дампа разрешён, это не билд.

## Инфраструктура памяти (Claude)

Hindsight self-hosted (podman: hindsight 8888/9999/8080, embeddings,
reranker; бэнк chronos-ecosystem). Две ловушки: (1) после краха сессии
умирает pasta port-forwarder — контейнеры «Up», порты с хоста мертвы →
`podman restart` всех трёх + `/reload-plugins`; (2) pasta форвардит
только IPv4 — в ~/.hindsight/claude-code.json URL должен быть
`http://127.0.0.1:8888`, НЕ localhost (Python резолвит в ::1 → вечный
Errno 104). Fallback для retain: REST
`POST /v1/default/banks/chronos-ecosystem/memories` (document_id в
батче должны быть уникальны).

## История (сжато; подробности — в minion-файлах и git log)

- Коммиты: 54747fc74 (хотфиксы+Фазы 3.0-3.2) → 240a0f51b (kvarn-активация,
  Фаза 3) → 76e57c616 (Фаза 4: scheduler split-graph + CLI kvarnN) →
  81729eb43 (hybrid-iswa passthrough, DSv4 касты, MoE prefetch) →
  f2afa0a13 (доки, эталон v3). Все — parity 13994 + bench стабильный.
- Хроника сегодняшних крашей (все закрыты, кроме последнего): (1)
  SET_ROWS/Q4_K — мост cparams.kvarn отсутствовал + fallback-типы
  K-суперблоки; (2) SET_ROWS через llama_memory_hybrid — гибрид не
  принимал kvarn; (3) SIGSEGV build_input_v_idxs — затенение методов
  kvarn-контекста (dynamic_cast-диспетчеры); (4) валидация «no native
  ops» — не было регистрации в CUDA (2.2d); (5) fit-сегфолт — статус
  игнорировался (H-9/O-5); (6) GGML_ASSERT KVARN_VIEW на decode —
  обвязка CUDA-графа (2.2e); (7) GGML_ABORT seq_rm p0=30 — ТЕКУЩИЙ,
  решение O-6.
- Уроки процесса: диффы миньонов проверять на вложенность скобок, а не
  только баланс; отчёты сверять с деревом построчно; прямые правки кода
  Архитектором — только однострочная механика, всё остальное через
  миньонов.
